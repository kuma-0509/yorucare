# ヨルケア開発管理表

進捗は `未着手`、`進行中`、`改善中`、`確認待ち`、`保留`、`後回し`、`判断済み`、`完了` のいずれかで記載し、完了した課題だけ完了日を `YYYY-MM-DD` 形式で記載する。

この管理表には、個人情報、利用者が入力した情報、センシティブな情報、個人を特定できる情報を記載しない。

| 課題 | 改善方法 | 進捗 | 完了日 |
| --- | --- | --- | --- |
| 生成AIなどへ共有で、表計算ソフト向けのCSVを保存できない | 全文確認後に、選んだ期間・項目だけをUTF-8（BOM付き）のCSVとして保存できるようにする。テキストの全文確認・コピー・txt保存は変えない。選んでいない項目がCSVに出ないことと、日本語が読めることをテストで固定する | 完了 | 2026-09-10 |
| 記録一覧が1日ごとのカードで、日付を横断して見比べにくい | 「これまで」を直近7日の表（行＝日付、列＝気分・睡眠・メモなど）に変える。未記録の日は空欄の行として並べ、メモは省略せず改行を保つ。詳しく見る・編集・記録をつける操作は表から到達できることを表示テストで固定する | 完了 | 2026-09-10 |
| 記録画面の寝た時間・起きた時間が毎回空欄から始まる | その日の記録がまだないときだけ、直近に保存した寝た時間・起きた時間を初期値として入れる。すでに保存済みの日は保存値を優先し、初期値から変更して保存できる。端末内だけに残し、送信しない。すべての記録の削除とバックアップ取り込みの成功時は初期値を消し、取り込みに失敗して戻したときは残す。空の新規入力と既存記録の復元を表示テストで固定する | 完了 | 2026-09-09 |
| 「テキストファイルを保存」で書き出した共有テキストが端末で文字化けする | 保存ファイルをUTF-8（BOM付き）など、端末の標準ビューアが日本語として開ける形式にする。画面上の全文確認とコピーの内容は変えない。保存して開いたときに日本語が読めることをテストで固定する | 完了 | 2026-09-09 |
| 生成AIなどへの共有で、一度に出せる期間が7日間までに限られている | 上限を1か月（30日）に広げ、画面案内、`docs/sharing-decision.md`、期間の検証、実機チェック項目を同じ上限へ合わせる。端末内でテキスト化し、全文確認後だけ共有する経路は変えない | 完了 | 2026-09-09 |
| 記録画面に復職後の経過が日数目として出ない | 「書く」の見出し「今日の記録」の上に、設定済み復職日を1日目として「復職後○日目」を出す。未設定・復職日前の日付では出さず、日付切替で対象日に合わせて変わることを表示テストで固定する。公開中の画面で見えることを確認してから完了にする | 完了 | 2026-09-08 |
| カスタム入力で「やらないこと」を記録項目として選べない | 「できたこと」と対になる任意項目として「やらないこと」を追加し、選択状態をこの端末だけに保存する。非表示にしても過去の記録と登録内容は保持し、再表示できることをテストで固定する | 完了 | 2026-09-07 |
| セルフケア画面がカスタム入力の選択状態に関係なく表示される | カスタム入力で「できたこと」または「やらないこと」の少なくとも一方を選んだ場合だけ、第3ペインと下部タブを表示する。両方とも未選択になった場合は安全に「書く」へ戻ることを表示テストで固定する | 完了 | 2026-09-07 |
| セルフケア画面で「できること」と「やらないこと」をまとめて管理できない | 第3ペインに両方の登録一覧を常に表示し、それぞれ追加・編集・削除できるようにする。片方だけをカスタム入力で選んだ場合も両方を管理でき、同名項目を種類ごとに独立して扱えることをテストで固定する | 完了 | 2026-09-07 |
| 第3ペインの下部タブ名が画面内容を十分に表していない | 下部タブと画面見出しを「セルフケア」へ変更し、「できること」と「やらないこと」の管理画面であることが伝わる案内文へ更新する | 完了 | 2026-09-07 |
| ローカルで完了した変更が本番サイトへ反映されないまま完了報告された | 最新の本番ブランチを起点に実装・テスト・本番ビルド・リモート反映・公開URLの表示確認までを一連の完了条件とし、公開中の画面で要件を満たすことを確認してから完了へ変更する | 完了 | 2026-09-08 |
| 記録一覧で入力済みメモを確認するために詳細画面を開く必要がある | 「書く」で保存したメモを「これまで」の一覧表のセルへ省略せず改行を保って表示する。メモが空欄の場合は空欄のままにし、保存から一覧表示までを結合テストで固定する | 完了 | 2026-09-07 |
| Phase 2 の目的と実装優先順位が未決定 | 検証目的、指標定義、投資判断ゲート、実装優先順位を `docs/phase2-plan.md` に確定する | 完了 | 2026-07-25 |
| 匿名利用イベントが永続化されておらず継続指標を集計できない | Neon Postgresへ許可済みイベントを保存し、IDの不可逆化、重複防止、日次集計、Week 2・Week 4集計、任意同意、停止時削除、365日後の自動削除を実装する | 完了 | 2026-07-26 |
| 匿名利用イベントの主要指標が5件のうち3件しか集計できず、集計を実行する手段もない | `docs/phase2-plan.md` 3.2節の主要指標のうち、週あたり記録日数（中央値）と中断後再開率を `src/lib/analytics-retention.ts` へ足し、既存3件と同じ Day 0〜Day 27 の窓で数える。記録が0日の週も中央値の母数に入れ、窓の終わりまで記録が戻らなかった対象も中断の分母に数える（再開した側だけを分母にすると再開率が必ず100%になる）。分母へ入れるのは Day 27 が終わってからにする（当日に数えると、その日のうちに届いた保存で報告済みの値が後から変わる）。読み出しは接続文字列を持つ管理環境で実行する `pnpm analytics:summary` に置き、公開されるエンドポイントを増やさない。アプリ側にあった未使用の読み出し関数は消して経路を1つにする。出力は集計値だけとし、割合には母数、母数が0なら「出さない」、対象が15件未満なら Gate 1 の判定を出さない旨、集計上の限界を併記する。あわせて、直近7日と説明しながら全期間を数えていた端末内の記録日数の集計も直す | 完了 | 2026-08-18 |
| データ操作層が将来の API 化に必要な非同期境界へ対応していない | `src/lib/repository.ts` のインターフェースを定義し、呼び出し側を段階的に `Promise<Result<T>>` 対応へ移行する | 完了 | 2026-07-26 |
| アカウントとクラウド保存の構成が未決定 | 認証、アクセス制御、暗号化、削除、移行の要件と採用構成を `docs/account-cloud-storage-decision.md` に確定する。2026-09-08に、複数端末同期を外した最小構成（クラウドバックアップと復元だけ、同期する端末は1台、記録全体を1スナップショットで保存、親鍵はVercelの非公開環境変数、自動期限削除なし）へ改訂し、フラグOFF・本番画面から到達不可のまま実装に着手できる状態とした | 完了 | 2026-09-08 |
| リマインドの導入判断と最小構成が未決定 | 導入条件、通知手段、ON/OFF、時刻、文面、停止、データ境界、受入条件を `docs/reminder-decision.md` に確定する | 完了 | 2026-07-27 |
| Gate 2通過後のリマインド機能が未実装 | 固定選択フィードバックの30%以上が「忘れる」または「始めるきっかけがない」に該当した場合に限り、`docs/reminder-decision.md` の最小構成を実装して実機検証する | 保留 | — |
| 本人の選択に基づく共有機能が未実装 | 最大7日間・選択項目のテキストを端末内で生成し、全文確認後だけOS共有シート、コピー、`.txt`保存を可能にする。対人共有の要件は `docs/sharing-decision.md` に確定する | 完了 | 2026-07-31 |
| Gate 4通過後の対人共有用期限付きリンクが未実装 | 需要と安全要件を確認後、本人の明示操作、項目選択、短い有効期限、失効、閲覧制御を備えた共有リンクを `docs/sharing-decision.md` に従って実装する | 保留 | — |
| 相談窓口へつながる導線が未実装 | 全タブ共通ヘッダーに相談先を常設し、緊急通報、公的な相談窓口、仕事・復職の相談、医療相談の境界を `docs/consultation-routing.md` に整理する。記録を送信せず、本人の操作だけで電話・公式サイトを開く | 完了 | 2026-08-02 |
| 東京都の地域相談先をアプリ内で探せず、提出資料に記載した4つのオープンデータが未利用 | 特別区、市町村、中核市・政令市の保健所・保健センター一覧を統合し、本人が選んだ区市町村の窓口を住所・電話番号つきで表示する。地域選択は保存・送信せず、現在地も取得しない。令和6年度東京都福祉保健基礎調査は、相談先を見つけやすくする背景資料として母数と割合を明記し、4データすべての公式出典を画面内で確認できるようにする | 完了 | 2026-08-22 |
| ふりかえりの週次まとめが追加予定のまま | 直近7日の記録を定型ルールだけでまとめ、ふりかえりタブに常設する。記録がなかった日を失敗として書かず、評価語と助言を出さず、数値には母数を添え、割合ではなく日数で示す。空状態と表現の制約を含む表示テストを追加する。共有は端末内でテキスト化してコピーするだけとし、送信しない | 完了 | 2026-08-03 |
| 週次まとめに目標の達成状況を含められない | 期間集計へ目標のふりかえりを加え、週次まとめに「目標のふりかえり」の行を出す。選択式の達成状況だけを結果ごとの日数で数え、本人が書いた目標の文面は集計にも表示にも使わない。母数はふりかえりを選んだ日数とし、割合では示さず、できなかった日数を項目として並べない | 完了 | 2026-08-14 |
| AI機能で外部へ渡してよいデータ範囲が未決定 | 集計値、選択式のタグ、辞書のタイトルだけを渡し、本人が書いたメモ本文は端末外へ出さない方針を `docs/ai-consent-decision.md` に確定する。方針は運用ルールではなく、戻り値型に本文のフィールドを持たせないことで保証する | 完了 | 2026-08-01 |
| 期間集計をアプリ側で確定させる仕組みが未実装 | `src/lib/ai/period-summary.ts` に期間集計を実装し、暦日数と記録があった日数を別々に返す。項目ごとに母数を持たせ、対象が0件のときは0ではなくnullを返して「値が低い」と「データがない」を区別する。就寝時刻は日をまたぐため正午を基準とする窓で中央値を取る | 完了 | 2026-08-01 |
| 集計値を言語化する残りのツールが未実装 | タグ・辞書項目の頻度集計、連続日数の検出、セルフケア実行日と非実行日の差分を実装する。差分の戻り値は相関を因果と読ませないよう、効果ではなく事実だけを述べるキー名にし、片側の日数が少ない場合は差分を返さない。連続日数は未記録日で途切れさせ、記録できなかった期間を好調が続いた期間として扱わない。読み取り専用とし、保存・削除の手段は用意しない | 完了 | 2026-08-01 |
| LLM呼び出しと報告書出力が未実装 | 数値はアプリが埋め、文章の穴だけをLLMが書く穴埋め方式にする。報告書の骨格、穴を埋める境界、プレーンテキスト出力までを実装する。文章の穴はアプリが用意した候補から選ばせる形にし、自由記述を受け取らない。禁止語の一覧は取りこぼすため、候補そのものを検査する用途に限る。載せる項目は `docs/sharing-decision.md` 5節の初期版に限り、書き込み系の操作はLLMに渡さず、確定は本人のボタン操作に限る。外部AI APIへ記録内容を送る実装は Gate 3 と別途のデータ取扱い判断まで行わず、既定で無効・本番画面から到達不可のまま据え置く。面談準備シートの「AIの出力メモ」欄は、本人がコピーして自分の生成AIへ貼り、返ってきた文章を貼り戻す経路で満たす。アプリ内からの自動生成をこの欄へ入れる場合は本課題の判断に従う | 保留 | — |
| スマートフォン実機テストの実施結果が文書化されていない | `docs/smartphone-test-checklist.md` を現行実装（3段階の入口、目標のふりかえり、自分メンテ、週のまとめ、積み重ね表示、相談導線、共有）に合わせて更新したうえで、iPhone と Android で実施し、同ファイルの「実施結果の記録」へ入力内容を含めずに結果と一般化した不具合だけを記録する。実施前の準備は済ませた（レビュー同意の全画面ダイアログを踏まえた無誘導計測の開始条件、見本データの生成手順、復元まわりと節目の見出しの記載修正）。残るのは実機2種・参加者5人・実施者の時間の手配と、公開URLが最新版かの確認で、いずれも開発作業では用意できない | 確認待ち | — |
| ふりかえりタブの「これから追加予定」が、実装済みの週のまとめを予定として説明している | 追加予定の説明を面談・通院前の準備だけに絞り、画面文言を `src/lib/copy.ts` へ集約する。週のまとめ、積み重ね、体調の傾向を予定として挙げないことを表示テストで固定し、実機チェックリストの既知不具合扱いを外す | 完了 | 2026-08-18 |
| 実機テスト用見本データの連続日数テストが日本などUTCより進んだ環境で失敗する | ローカル日付をUTC文字列へ変換して連続日数を自前計算するテストをやめ、製品と同じ週次まとめの集計経路で「最長3日」の表示契約を検証する。記録がない日を含む見本であることも集計結果から確かめる | 完了 | 2026-08-18 |
| 翌日に目標や行動実験を振り返る導線が弱い | 記録時に明日の目標または小さな行動実験を設定できるようにし、翌日の記録開始時に自動表示して「できた／できなかった／一部できた」を選べるようにする。未達時は行動を小さく具体化する補助質問や提案を表示する | 完了 | 2026-08-13 |
| 入力項目が多いと毎日の記録を続けにくい | 記録開始時に「よい／ふつう／しんどい」の3段階から状態を選べるようにし、選択に応じて追加質問を必要最小限に絞り、短時間で完了できる入力設計にする | 完了 | 2026-08-08 |
| 記録した状態から実行しやすいセルフケア行動へつなげる仕組みが不足している | 「自分メンテ」機能を設け、記録した状態に応じて実行しやすいセルフケア案を表示し、実施可否と実施後の感想を簡単に記録できるようにする。提案は3段階の状態と選択式のタグだけを入力とする定型ルールで組み、外部通信を使わない。選んだ案は「できること」へ登録して実施として記録し、感想は選択式だけを持つ。診断・治療・危機判定に読める表現を出さないことを表示テストで固定する | 完了 | 2026-08-15 |
| 継続や積み重ねを実感しにくくモチベーションを保ちにくい | 本人が設定した復職日を起点に、記録日数、活動日数、経過日数、最長の連続記録をふりかえりタブで可視化する。起点が未設定なら最初の記録日を既定にする。中断で減らない累計を上に置き、いま続いている日数は表示しない。節目は記録日数の累計と起点からの経過日数だけに置き、節目をねぎらう自動メッセージは定型ルールだけで組んで外部通信を使わない。評価語・助言・診断に読める表現を出さないことを表示テストで固定する | 完了 | 2026-08-15 |
| 積み重ねの節目をねぎらう文が、届いた日以外にも表示される | `src/lib/ai/milestones.ts` の `collectMilestones` は `justReached` を累計値の一致だけで判定するため、記録日数が節目に達した後、次の記録を書くまで毎日同じ文が出る。記録日数の節目は、その日に記録が追加されたときだけ対象にする。経過日数の節目は日付で変わるため現行のままとする。届いた日だけ出ることを表示テストで固定する | 完了 | 2026-08-16 |
| 実機テストの手順が「補助なしで2分以内」の測定として成立していない | `docs/smartphone-test-checklist.md` は、選ぶ状態と保存ボタンの位置を明示したまま所要時間を測る構成のため、`docs/phase2-plan.md` 3.3節の判定基準を測れない。参加者に渡す最小プロンプト（操作を教えない）と実施者用チェックリストを分け、無誘導の計測を先に行う。あわせて、保存後の演出が完了状態に入ると他の選択肢を出せないための実行順とリセット手順、週のまとめの母数を行ごとに持つ旨、復職日を持たない旧形式の見本の用意を直す。項目IDは付け替えない | 完了 | 2026-08-16 |
| しんどさのサインを解除しても、選んだタグが自分メンテの案に残る | 同じ段階をもう一度選んで解除したとき、`warningLevel` だけが未選択に戻り `warningTags` が残るため、画面に出ないタグが提案の絞り込みに効き続ける。解除時にタグも消すか、タグを表示している間だけ提案へ渡す | 完了 | 2026-08-16 |
| 「できること」を削除しても、その記録に実施後の感想が残る | 登録簿から項目を消すと各記録の実施IDは消えるが、実施後の感想が残る。画面には出ないのに記録が空でないと判定され、以降の編集でも残り続ける。実施IDがなくなった記録は感想も消す | 完了 | 2026-08-16 |
| 同じ名前の「できること」があると、自分メンテの案の選択を外せない | 案の登録は名前が一致する最初の項目を選ぶ一方、選択中かどうかは名前が一致するいずれかのIDで判定するため、2件目だけを選んだ状態で押すと1件目が足されて選択が外れない。選択中の項目を先に探すか、登録簿で名前の重複を防ぐ | 完了 | 2026-08-16 |
| 取り込んだ復職日が暦日として妥当か検証していない | 日付の検証が書式の確認だけのため、バックアップに `2026-02-31` のような存在しない日付が入っていても取り込める。取り込んだ後は、範囲の抽出と経過日数の計算で別の日付として扱われ、累計と経過日数が食い違う。取り込みの境界で暦日としての妥当性も確認する | 完了 | 2026-08-16 |
| R3F版「本棚にしまう」演出の品質をさらに高める必要がある | 本ごとに非均一スケールを適用し、収納後の小さな沈み込み（settle）を加える。背表紙、ページ束、金装飾を強化し、カメラを「筆記 → 本を閉じる → 背表紙 → 棚へ収納 → 温かな余韻」の流れに整える。閉じる音と接触音を調整し、全体を約7.4秒のタイムラインに仕上げる。棚姿勢では本の幅が奥行きへ、厚みが背表紙の幅へ回るため、非均一スケールと回転の軸の対応、および背表紙が手前を向く向きを表示テストで固定する。背表紙の装飾はふくらみの外面を基準に置き、題箋は金を縁として後ろに、暗い革の札を前に重ねる。画面が縦長のときに世界座標で幅の決まっている文字と本がはみ出すため、書いた行の大きさと背表紙を見せる段のカメラ距離を縦横比から決める | 完了 | 2026-08-18 |
| 縦長画面の筆記行が修正後も数px見切れ、収納奥行きの計算にも小さな誤差が残る | 文字の縮尺は本の原点ではなく、傾いた表紙上で最もカメラに近い位置までの距離から求める。棚の奥行きはテスト専用の近似値ではなく、背表紙を構成する実際の箱のローカルX幅を含めて算出する。390×844と640×900で行の左右端を描画確認し、距離と実寸の対応を単体テストで固定する | 完了 | 2026-08-18 |
| 3D演出の経過時間が React の状態として毎秒約31回更新される | 経過時間を状態に持つ `setInterval` をやめ、段（phase）の切り替えだけを状態に残す。段の中の進み具合は `performance.now()` から読む関数として渡し、読む側が `useFrame` の中で呼ぶ。タイマーと React のコミットがずれた瞬間に旧段の見た目へ次段の進捗を適用しないよう、読む側は現在描画している段を関数へ明示する。段の途中で描き直しが要る箇所（書いた行の増え方、行を消す位置、棚の接地影と収まった本を出す境目）は、変わる回数が数えられるので境目ごとの真偽値だけを状態に持つ。連続して変わる値（にじみ、背表紙のラベル、めくれるページ、接地影の濃さ）は、メッシュ・マテリアル・DOM を毎フレーム直に書き換える。効果音は描画とは別に時刻で予約し、フレームが飛んでも合図を飛ばさない。予約したタイマーが溜まってまとめて発火したときは、鳴らす時点の音声時計に載って束になるため、演出の時刻から離れた合図は捨てる（捨てる幅は合図どうしの最小間隔より小さくする）。**描画1フレームあたりの負担は変わらないため、実機での発熱・フレーム落ちに効くかは未測定** | 完了 | 2026-08-18 |
| R3F版「本棚にしまう」改善版のユーザー確認が必要 | 収納の向きと寸法、背表紙の題箋、縦長画面での見切れを直した現行版を `/preview/effects` で利用者視点から確認してもらい、収納位置、影、カメラ、音、余韻のフィードバックを収集して必要な微調整を行う。背表紙を見せる段は暗い書庫のトーンを保つため主光源から斜めを向いており、明るさが足りないという声が出た場合は光の当て方を見直す | 確認待ち | — |
| R3F版「本棚にしまう」演出の実機品質とパフォーマンスが未確認 | 本番フローへ統合したため、記録を保存して「書庫にしまう」を選ぶ経路で確認する。PCとスマートフォンから描画崩れ、フレームレート、読み込み時間、操作性を検証し、3D版・平面版・動きを控える設定の3通りが端末ごとに正しく分かれることを見る。音の入切と、音を出した状態での重さも確認する。手順は `docs/smartphone-test-checklist.md` の B-6・B-10〜B-18 に置いた。B-18 は画面の縦横比で変わる見切れ（書いた行が切れない、収納後の背表紙が棚に収まって見える）を実機で見る項目とする。問題が出る場合は軽量版またはフォールバック表示を強化する | 確認待ち | — |
| 本棚演出の質感と没入感をさらに高品質にする余地がある | GLBは採用せず、法線マップ・粗さマップと弱い環境光をその場で生成して、革・紙・木・金の光の返り方を素材ごとに分ける。全画面のポストプロセスは入れず、ビネットはDOMの重ね、Bloomは加算合成の板で代え、DOFは見送る。端末の段階に応じて解像度・影・粒の数だけを下げ、見た目の作り込みは端末で変えない。効果音は全体の音量を一段下げて角を落とし、鳴らすかどうかは端末内の設定（既定OFF）として持ち、AudioContextを本人の操作の中で作って自動再生ブロックへ対処する | 完了 | 2026-08-17 |
| 平面版の本棚演出にも、押しても鳴らない音の切り替えが表示される | 3D版だけが音の合図を持つため、WebGLが使えると確定した場合だけ音の切り替えを表示する。3D描画が途中で失敗して平面版へ切り替わった場合も非表示へ戻し、3D版・平面版の表示テストと実機チェック項目を更新する | 完了 | 2026-08-18 |
| 記録完了演出を本番フローへ自然に統合できていない | `BookShelfCinematic` から `CompletionChoice` への本番フローを統合し、CSS版、R3F版、reduced-motion設定時の表示ルールを設計して既存簡易演出との置き換えを判断する。`completion-log.ts` の単体テストを追加し、演出中の文言と完了後の案内を最終調整する | 完了 | 2026-08-17 |
| 本棚以外の「手放す」「浄化する」感覚を表現する記録完了演出を比較検証できていない | 本棚演出のトーン確定後に、「ゴミ箱へ捨てる」演出と「記録が溜まったら燃える」演出のプロトタイプを作成して比較する | 後回し | — |
| すべての記録を削除しても締めくくり演出の記録が端末に残る | `deleteAllRecords` が記録本体のキーだけを消し、締めくくり演出の選択を保存した `completionLog` キーを残していた。共有端末で使い終わったときに、どの日に記録したかが日付の一覧として端末へ残る。演出の記録を消す手段を `completion-log.ts` 側に用意し、全削除で一緒に消す。削除されることを `repository.test.ts` の移行テストと同じ場所で固定する | 完了 | 2026-08-18 |
| 1件の記録を削除しても同日の締めくくり演出の記録が端末に残る | 個別削除でも同日の演出の選択と手放し済み状態を先に消し、削除済みの日付が端末や「紙」の枚数へ残らないようにする。別の日の演出記録は維持し、最後の1件なら保存キー自体を消す | 完了 | 2026-08-20 |
| まとめて手放す演出を途中終了しても予約タイマーが残る | フェーズ切り替えと自動完了のタイマーを保持し、スキップ時とアンマウント時にすべて解除する。スキップ後に完了処理が再実行されないことを表示テストで固定する | 完了 | 2026-08-20 |
| まとめて手放す演出のスキップ操作が支援技術から隠されている | フォーカス可能なスキップボタンの祖先から `aria-hidden` を外し、演出中も読み上げと操作の対象になることを表示テストで固定する | 完了 | 2026-08-20 |
| 実機テスト手順の全削除後に残るデータの説明が現行実装と一致しない | `docs/smartphone-test-checklist.md` の無誘導計測準備を、全削除で記録本体と演出履歴が消え、初回表示、同意、できること、復職日、音設定は残る現行仕様へ合わせる | 完了 | 2026-08-20 |
| まとめて手放す導線のしきい値が画面側と定数で二重管理になっていた | `WeeklyBurnBanner` が枚数のしきい値を画面側へ直書きしており、`WEEKLY_BURN_THRESHOLD` を変えても導線の出る条件が変わらなかった。判定を `canBurnWeekly` に寄せ、しきい値を定数1つで決まるようにする | 完了 | 2026-08-18 |
| まとめて手放す演出の文言がコンポーネントへ直書きされていた | 枚数の案内、手放す操作、記録が消えない旨、スキップの計7か所が `src/lib/copy.ts` を経ずに書かれており、言い換えの単一基準から外れていた。文言を `copy.ts` へ移し、枚数は前後の語で挟んで組み立てる | 完了 | 2026-08-18 |
| 完了とした実装が版管理へ反映されないまま端末内に残っていた | 完了扱いの成果物を課題単位のコミットに分割してリモートへ反映する。一時作業ディレクトリ、別リポジトリとして管理している静的サイト、binaryの原本は追跡対象から除外し、型検査の対象も本体に限定する | 完了 | 2026-08-01 |
| 本棚演出の方向性確定前には着手しない項目がある | 外部HDRとdreiのEnvironment presetは利用せず、`/preview/completion` の「粒が集まる演出」は再開せず、本棚トーンの確定までは「ゴミ箱へ捨てる」「記録が溜まったら燃える」演出の実装に着手しない。本番データ構造の `DailyRecord` と `schemas.ts` を変更しないという制約は、記録完了演出の作業に対するものであり、記録・ふりかえり本体の機能実装を縛らない（2026-08-15 確認） | 判断済み | — |
| 実機テストの結果、書庫にしまう／紙にして手放すの演出は不要と判断された | 記録の保存後は演出も選択肢も挟まず、そのまま完了画面へ進む形へ戻す。`CompletionChoice`、`BookShelfCinematic` と R3F 一式、`PaperTrashAnimation`、まとめて手放す演出（`WeeklyBurnAnimation`・`WeeklyBurnBanner`）、`/preview/effects`、演出の音設定（`completion-sound.ts`）と対応するテストを削除し、`copy.ts` の演出専用の文言も外す。演出ログ（`completion-log.ts`）は書き込む経路が無くなるが、旧版を使っていた端末にはどの日に記録したかが日付の一覧として残るため、記録の個別削除と全削除で消す入口だけを残す。使わなくなった three・@react-three の依存、演出用の CSS アニメーション、動きを控える設定の判定も外し、`docs/smartphone-test-checklist.md` の B 章と定性の項目を演出なしの現行フローへ合わせる | 完了 | 2026-08-21 |
| 記録画面の入力項目を本人が選べず、使わない項目も毎日表示される | ヘッダーの相談先ボタンをハンバーガーメニューへ変え、その下に相談先とカスタム入力を置く。カスタム入力では自分メンテ、くわしく書く、しんどさのサイン、できたこと、小さな目標の5項目を端末内の設定で表示・非表示にでき、既定はすべて非表示にする。日付、状態、寝た時間・起きた時間、メモはいつも表示し、睡眠の入力は折りたたみから独立させる。非表示にしても保存済みの記録は消さず、表示へ戻せばまた見えるようにする | 完了 | 2026-08-21 |
| 無料モニターから個人有料版・企業受注へ進む事業化の道筋が文書化されていない | 逆算スケジュール、エビデンス設計、有料版の採算、企業受注ルート、助成金の使い分け、続ける・やめるの判断基準を `docs/business-plan-return-to-work.md` に確定する | 完了 | 2026-09-03 |
| クラウドバックアップと復元が未実装で、端末を失うと記録が戻らない | `docs/account-cloud-storage-decision.md` の最小構成に従い、Managed Better Authの6桁コード（Email OTP）による本人確認、スナップショットの暗号化保存、復元、クラウド停止・退会時の削除を実装する。新規登録は無効にし、登録した参加者だけがログインできる状態にする。記録の保存成功とクラウド送信の成功を別の状態として扱い、送信失敗は画面へエラーを出さず再送する。端末とクラウドの双方に記録がある復元では自動統合せず、JSONバックアップの保存を必須にしてから本人に選ばせる。初期OFF・本番画面から到達不可のまま同文書12節の検証項目を通す。テーブル定義、暗号化と親鍵の入れ替え、保存・取り出し・削除・端末登録のAPI、端末側の送信と復元の判断、認証基盤（Managed Better AuthのEmail OTP）のセッション検証の組み込みまでを実装済み。Managed Better Auth は2026-09-08時点で新規登録の抑止をConsole設定だけでは保証していないため、アプリ側でも許可済みメールアドレス一覧との突き合わせを二重に行う。Neon Console側の設定は2026-09-11に実施済み（本人記録用プロジェクト作成、Managed Better Authの有効化、Email OTPの確認方法設定、SNSログイン・組織機能の無効化、`NOBYPASSRLS` の実行時ロール作成と権限付与、マイグレーション適用）。実施中に、Console・API・CLIで作るDBロールは`BYPASSRLS`を持ちRLSを素通りするため実行時ロールはSQLで`NOBYPASSRLS`を明示して作る必要があること、新規登録を止める設定はConsoleに存在しないこと、認証メール（Email OTP）は1分以内にGmailへ到達すること（他事業者は未確認）を実測で確認した。クラウド保存の設定画面と復元画面（預ける前の説明、ログインとアップロードの分離、預ける直前の件数表示、最終預け日時、引き継がれた端末の案内、復元時の選択、停止と退会）も実装済み（2026-09-12、最新mainへ統合）。停止・退会の削除APIは直近の本人確認を必須にしている。2026-09-13に、Preview環境（ブランチ名入りの固定URL）でダミーデータによる通し確認を完了した（ログイン／許可リスト外の拒否／預ける／最終預け日時／復元／停止／退会＝ログアウトまで、別端末での復元、Gmail以外の事業者＝Outlookへの6桁コード到達）。残りは`yorucare_app`ロールのパスワード再作成（作成時のSQL実行履歴に残るため）、退会時の認証アカウント削除（Managed Better Authの`delete-user`を有効化できるか未確認）、フラグを開ける前の同意文面・手続き確認 | 進行中 | — |
| クラウドバックアップのログイン確認画面が、サインアウト失敗を画面に反映しない | `src/app/cloud-login/page.tsx` の `handleSignOut` が `signOut()` の戻り値のエラーを見て判定するよう直した。失敗（例: Neon Console の「ドメイン」未登録による `403 INVALID_ORIGIN`）や例外のときは、未ログイン表示へ切り替えず、その場のフェーズ（ログイン済み／許可リスト外）を保ったまま「ログアウトできませんでした」と表示する。成功したときだけ未ログイン表示へ進む。回帰確認として、修正前のコードでは新設のテスト9件中7件が実際に落ちることを確認した | 完了 | 2026-09-12 |
| クラウドバックアップのログイン確認画面が、許可リストの結果を見ずに「ログイン済みです」と表示する | 画面の判定を、Better Authのセッション有無だけを見る `cloudAuthClient.getSession()` から、許可リストの突き合わせまで行う新しい `GET /api/cloud/session`（`getCloudAuthStatus` を経由）へ差し替えた。サーバー側は3状態（`ok`／`not_allowed`／`unauthenticated`）を返し、画面はBetter Authのセッションはあるが許可リスト外のときに「このメールアドレスはクラウド保存の利用対象に登録されていません」という専用の案内を出すようにした（従来の「ログイン済みです」は出さない）。`getCloudSession` は既存の契約（許可リスト外はnull）を保ったまま、この新しい判定を内部で再利用する形にした | 完了 | 2026-09-12 |
| クラウド保存の設定画面も、許可リストの結果を見ずに「預ける前の説明」を表示する | 統合直後にローカルの実機（ブラウザ）確認で発見。`cloud-backup-panel.tsx` がログイン画面と同じ問題を抱えていた（`cloudAuthClient.getSession()` でBetter Authのセッション有無だけを見ており、許可リスト外でも「ログイン済み」として預ける操作を進められた。実際に預けようとするとサーバーが401で拒否し、原因の分からない「いまは預けられませんでした」に化けていた）。ログイン画面の修正で使った `GET /api/cloud/session` の判定ロジックを `src/lib/cloud-auth-status.ts` として共通化し、設定画面・ログイン画面の両方がこれを使うように直した。設定画面には「このアカウントでは使えません」という専用の案内を追加した。回帰確認として、修正前のコードでは新設のテスト3件が実際に落ちる（許可リスト外でも「この内容を預ける」の確認画面を出してしまう）ことを確認した | 完了 | 2026-09-12 |
| クラウド保存の設定画面の「退会する」が、サインアウト失敗を画面に反映しない | ユーザーによるPreview実機確認で発見。`cloud-backup-panel.tsx` の `handleDelete`（`kind === "leave"`）が `cloudAuthClient.signOut()` の戻り値の `error` を見ず、失敗しても必ず「ログインから出た」表示に切り替えていた（ログイン確認画面の`handleSignOut`で2026-09-12に直したのと同じ握り潰し方。こちらは独立した実装だったため直っていなかった）。クラウド上の控えを消したあとサインアウトに失敗すると、サーバー側のセッションは生きたままなのに「クラウド上の控えを消して、ログインから出ました」と表示し、実機確認では実際にログイン確認画面（`/cloud-login`）を開くと「ログイン済みです」のままになっていた（そこでのログアウトも同じ原因で失敗した）。戻り値の`error`を確かめ、失敗・例外時は「ログインから出た」表示へ切り替えず、`not_enabled`（ログイン済みで預けていない）のまま「クラウド上の控えは消しましたが、ログアウトできませんでした」と表示するよう直した。回帰確認として、修正前のコードでは新設のテスト2件が実際に落ちることを確認した | 完了 | 2026-09-13 |
| Preview専用の固定IDが、本物のログイン状態と許可リストの判定を覆い隠す | ユーザーのPreview実機確認で「許可リストを空にしても表示が変わらない」という報告があり、原因を調べて発見。`getCloudAuthStatus` が `USER_DATA_DEV_OWNER_ID`（Preview専用の抜け道）を**いちばん先に**見ていたため、この変数が設定されているあいだは、ログインしていてもいなくても、許可リストに載っていてもいなくても、常に「使える」と答えていた。判定の順序を入れ替え、本物のログイン状態（セッション＋許可リストの突き合わせ）を先に確かめ、未ログインのときだけ固定IDへ落ちるようにした。さらに、固定IDで通ったときは `GET /api/cloud/session` が `devOwner: true` を返し、ログイン確認画面が「検証用の固定IDで表示しています」と本人に見せるようにした。回帰確認として、修正前のコードでは新設のテスト3件が実際に落ちることを確認した | 完了 | 2026-09-13 |
| クラウド保存のログアウトが必ず失敗する（415 UNSUPPORTED_MEDIA_TYPE） | Preview実機確認で「ログインはできるのにログアウトだけ必ず失敗する」という報告。原因は認証の中継（`src/app/api/auth/[...path]/route.ts`）が、許可リストの照合のために本文を読み直して要求を組み直すとき、本文が空でも `body: ""` を渡していたこと。`Request` はこのとき `Content-Type: text/plain;charset=UTF-8` を自動で付け、`@neondatabase/auth` の中継がそれを上流へ転送する。上流のBetter Authはルーター全体に `allowedMediaTypes: ["application/json"]` を設定しており、JSON以外のContent-Typeを415で断る。ブラウザのログアウトは本文もContent-Typeも持たないため、この経路だけが必ず失敗していた（コードの送信・検証は本物のJSON本文を持つため成功していた）。本文があるときだけ渡すよう直した。ローカルで偽の上流サーバーを立て、本物の`@neondatabase/auth`の中継部分を通して、修正前は`text/plain`が届き修正後は付かないことを確認し、`upstream-contract.test.ts`として残した | 完了 | 2026-09-13 |
| Preview環境で、いま見ている画面がどのデプロイ・どの設定で動いているか分からない | Vercelはデプロイのたびに新しいURLを発行し、環境変数はデプロイごとに焼き付くため、設定を直して再デプロイしても古いURLを開いたままでは何も変わらない。この見分けが付かず、実機確認で何度も時間を失っていた。検証用の状態確認 `GET /api/cloud/diagnostics` を追加し、`/cloud-login` の下部に折りたたみで表示する（本番では経路が404になり画面にも出ない）。返すのはコミットとブランチ、許可リストの**件数**、固定IDの設定有無と使用中か、認証基盤の**ホスト名**と到達可否、Cookieの鍵の有無、いまのログイン判定だけで、メールアドレス・鍵・URL全体は返さない | 完了 | 2026-09-13 |
| 自動送信が重なり、古い内容が新しい世代として残ることがある | PR #63の自動レビュー（P1）で指摘。`backupAfterSave` が保存のたびに結果を待たない送信を始めるため、続けて保存すると送信が重なり、先に作った古い内容があとから届いて新しい世代になりうる（復元すると、保存したはずの変更が消える）。送信を必ず1件ずつにし、送信中に来た依頼は「あとで1回」とだけ覚えて、終わってから最新の内容を作り直して送るようにした。回帰確認として、修正前のコードで新設テストが実際に落ちることを確認した | 完了 | 2026-09-13 |
| 記録以外の変更が、クラウドへ預け直されない | PR #63の自動レビュー（P1）で指摘。預けるのは記録全体のまるごと1件なのに、預け直しは `saveRecord` のときだけ動いていた。記録の削除・全削除、「できること」・「やらないこと」の追加／変更／削除、復職日の変更では動かず、**消したはずの記録がクラウドに残り、復元で戻ってきてしまう**。また「次の保存かアプリ起動のときに送り直す」とコメントにありながら、起動時の送り直しが実装されていなかった。端末側の変更をまとめている `src/lib/storage.ts` のすべての変更経路と、アプリ起動時（`app-shell.tsx`）から預け直すようにした。復職日だけは画面から `repository` を直接呼んでいたため、同じ経路へ通した | 完了 | 2026-09-13 |
| 戻したあと、預け直しが始まっていないのに「預ける端末にしました」と伝える | PR #63の自動レビュー（P1・P2）で指摘。(1)「クラウドから戻す」は預けることへの同意ではないため、戻しただけでは以後の変更が送られないのに、画面は「この端末をクラウドに預ける端末にしました」と伝えていた。文面を「続けて預けるには、下の『この内容を預ける』を押してください」に変えた。(2) 端末の登録（`claimThisDevice`）が失敗しても結果を見ずに成功として終わっていた。失敗時は専用の案内を出すようにした。いずれも回帰確認として、修正前のコードで新設テストが実際に落ちることを確認した | 完了 | 2026-09-13 |
| 日々の記録が0件だと、「できること」等しか無い人が何も預けられない | PR #63の自動レビュー（P2）で指摘。設定画面が記録の件数だけで「預ける」ボタンを閉じていたため、本人が直した「できること」・「やらないこと」・復職日しか無い人は、関係の無い記録を1件作るまで預けられなかった。判定を `isEmptyPayload`（記録・やらないこと・復職日・見本以外のできること）とそろえた | 完了 | 2026-09-13 |
| ログアウトが403で失敗しても、どのURLを登録すればよいか画面から分からない | 実機確認②で「（詳細: 403 feature_not_supported）」が出た。`@neondatabase/auth` は403をすべて `feature_not_supported` に機械的に置き換えるため、符号からは原因が分からない（読み取れるのは「403」だけ）。この案件で実際に起きた403はManaged Better Authの「Domains」未登録だった（2026-09-11に実測、ログインは通るのにサインアウトだけ失敗する）。Vercelはデプロイのたびに専用URLを発行し「Visit」もそれを開くため取り違えやすい。「検証用の情報」に「この画面のURL」の行を足し、そこに出る値をそのままDomainsへ登録できるようにした。あわせて、`/cloud-login` でログアウトが403で失敗したときだけ、Domains未登録の可能性を案内に添えるようにした（参加者向けの設定画面には出さない） | 完了 | 2026-09-13 |
| 検証用の情報が、ログイン・ログアウトの後も古い判定を出し続ける | 追加した「検証用の情報」が画面を開いた瞬間の値を出しっぱなしで、ログイン後も「いまのログイン判定: unauthenticated」のままだった。原因の切り分けに使う欄が逆に人を迷わせるため、表示（フェーズ）が変わるたびに取り直すようにした。回帰確認として、修正前のコードで新設テストが実際に落ちることを確認した | 完了 | 2026-09-13 |
| ログアウトに失敗しても、原因を切り分ける手がかりが画面に出ない | Preview環境は開発者ツールを開かないと応答が見えず、「ログアウトできませんでした」だけでは原因（許可リスト・ドメイン未登録・通信断）を切り分けられなかった。`describeAuthError` を追加し、ログイン確認画面と設定画面の両方で、案内文の末尾にHTTPの状態番号と短い符号（例:「（詳細: 403 INVALID_ORIGIN）」）を添えるようにした。メールアドレスや6桁コードは出さない。あわせて、認証の中継（`src/app/api/auth/[...path]/route.ts`）が本文の無い要求（ブラウザのログアウト）に長さ0の本文を足して転送していたのをやめた | 完了 | 2026-09-13 |
| クラウド保存の同意を「いつ・どの文面に同意したか」まで残していない | **記録のみ。いまは開発しない**（2026-09-14の運営判断）。現在の実装（`src/lib/cloud-consent.ts`）は端末内に `granted` / `denied` の文字列を持つだけで、同意した日時も、同意した時点の説明文の版も残らない。ブラウザのデータを消せば同意の記録も消える。同意を取った証拠として扱うには、(1)同意日時 (2)同意した説明文の版（文面を直したら版を上げる） (3)端末を変えても消えない置き場所、の3つが要る。(3)をどこまでやるかは、記録が要配慮個人情報に当たるかの確認結果（`docs/cloud-backup-consent-draft.md` 5.1節）で変わる。**説明文が確定してから着手する**（先に作ると版番号を付け直すことになる） | 未着手 | — |
| 運営から利用者へ知らせる手段がない（クラウド保存の提供終了の予告ができない） | **記録のみ。いまは開発しない**（2026-09-14の運営判断）。クラウド保存の説明文で「提供を終えるときは1か月前までにこの画面でお知らせします」と約束したが、その仕組みがない。このアプリにはPWA・Service Worker・Push購読・配信処理が無く、端末を閉じていても届く通知は意図的に未実装（`docs/reminder-decision.md`、Gate 2未達）。実現できるのは「アプリを開いたときに画面へ出すお知らせ」で、Push基盤は要らない。**アプリを開かない人には届かない**ため、運用（説明会・個別連絡）で補う前提。約束した以上、提供終了を決める前に用意する必要がある | 未着手 | — |
| クラウド保存を現行プログラムへ適用する際の同意文面と手続き確認の担当・期限が未定 | 参加説明、同意文面、研究・安全管理手続きへの影響を誰にいつまでに確認するかを決め、確認結果を `docs/account-cloud-storage-decision.md` の11.2節へ反映する。確認が完了するまで本番画面からクラウド保存へ到達できるようにしない。2026-09-14に `docs/cloud-backup-consent-draft.md` を作成し、作業を3つ（①参加者への説明と同意 ②研究としての扱い ③個人情報の安全管理・国外移転）に分解した。参加者へ配れる説明文の草案、確認相手ごとのそのまま送れる質問文、運営が決めるべき6項目、完了判定のチェックリストまで用意済み。2026-09-14に運営の方針が決まった（削除しない／問い合わせ先は熊谷祐希／緊急連絡先は案内しない／研究発表の予定あり・発表先未定／同意はGoogleフォームとアプリ内の2経路／提供終了は1か月前にアプリ画面で予告／フォームはオンラインサービス参加時に配布）。**②研究の確認は5か月プログラムの実施時、③個人情報の確認は後回しとし、先に本番へ出す判断をした**（`docs/cloud-backup-consent-draft.md` 8.2節）。本番へは2段階で出す。**第1段階（許可リストを運営者だけにした試運転）は2026-09-14に完了**し、本番URLでカード表示・ログイン・預ける・復元・退会まで確認した（参加者のデータは1件も預かっていない）。第2段階が参加者への提供で、③の確認・説明文の確定・フォーム作成・DBパスワード再作成を終えてから行う（手順は `docs/account-cloud-storage-decision.md` 11.3節） | 進行中 | — |
| Neon Auth の無料枠とマジックリンクの提供状況が未確認 | 公式資料で無料枠の上限、ログイン方式の提供状況、本人記録用に匿名分析とは別のNeonプロジェクトを立てる手順を確認し、確認日と結果を `docs/account-cloud-storage-decision.md` の2.1節へ記録する。確認の結果、製品名がManaged Better Authへ改称されベータであること、Freeプランの無料枠で足りること、リンク方式には自前のメール事業者が必須であることが分かったため、ログイン方式を6桁コードへ変更した | 完了 | 2026-09-08 |
| 同期する端末が切り替わったときに、以前の端末へ出す案内の文言が未定 | 停止状態になった端末に表示する文言を決める。引き継いだ日付を示し、端末内の記録は消えていないことと、この端末を再び同期端末へ戻す方法が分かる表現にする。責める表現や不安をあおる表現を使わない。引き継いだ日付、記録が消えていないこと、預け直す方法の3文を `copy.ts` に置き、利用者へ確認のうえ確定した | 完了 | 2026-09-09 |
| 検証終了後にクラウド上の本人データを全件削除する日付が未定 | 2026-09-14に**方針を変更**し、検証終了日での全件削除はしないことにした。機種変更で記録を失わないための機能なのに、運営の都合による期日で消えては目的を果たさないため、本人が消すまで預かり続ける。代わりに、提供を終える場合は事前に知らせることを参加説明とアプリの文面で約束する（アプリの文面 `src/lib/copy.ts` も修正済み）。残りは予告の期間と連絡手段を決めること（`docs/cloud-backup-consent-draft.md` 7.2節） | 完了 | 2026-09-14 |
| 暗号化の親鍵を入れ替える手順が未文書化 | 親鍵の漏えいが疑われた場合に、新しい版の作成、旧版での復号と新版での再暗号化、旧版の失効、セッション失効までの順序と担当を `docs/account-cloud-storage-decision.md` 7.4節へ文書化した。親鍵の版番号と入れ替え中の復号は既存テストで固定済み。全件の包み直しは記録本文を復号せず `pnpm user-data:rewrap-keys`（既定は確認のみ）。アプリ用ロールではRLSで全件が見えないため、ownerロールの接続文字列を手元だけに渡す。公開APIは増やさない | 完了 | 2026-09-14 |
| 認証メールの到達性が未確認で、共有の送信元のまま公開してよいか判断できない | 検証中は開発・検証用の共有送信元を使う。公開前に、参加者が実際に使うメール事業者へ6桁コードが届くか、迷惑メールへ振り分けられないかを実機で確認する。到達性が足りない場合は自前のメール事業者を追加し、保存期間、再委託先、国外取扱い、削除条件を契約前に確認する | 未着手 | — |
| 採用する認証基盤がベータで、公開判断の前提が変わる可能性がある | 公開の入口を開ける前に提供状態を再確認し、正式版へ移行したか、制約や上限が変わっていないかを `docs/account-cloud-storage-decision.md` の2.1節へ反映する。ベータのまま公開してよいかを判断してから入口を開ける | 未着手 | — |
| 許可済みメールアドレス一覧は本人データを見せない防御であり、アカウント作成そのものは止められない | 認証を中継するAPIで、コードの送信・検証を転送する前にも許可リストと照合し、一覧に無いメールアドレスは転送しない。一覧が未設定なら誰も通さない。これによりアプリ経由でのアカウント作成と送信量の消費を防ぐ。認証基盤のURLを直接呼ばれる経路までは塞げないため、検証が終わったら入口フラグを戻す運用は続ける。2026-09-11にConsoleの画面を確認したところ、新規登録だけを止める設定は存在せず、常設バナーで「近日対応」と明記されていることが分かった。当面この設定は備わらない前提で運用し、備わった時点で有効にする | 改善中 | — |
| 認証基盤のパッケージが要求するNext.jsの版が、本アプリの版より新しい | パッケージの `peerDependencies` は Next.js 16 以上を要求するが、本アプリは 15.5.18。インストール、型チェック、テスト、本番ビルドはいずれも成功し、現在使っている機能は動作している。公式に支援される組み合わせではないため、認証まわりで想定外の挙動が出た場合は最初にここを疑う。Next.js の版を上げるかどうかは別途判断する | 未着手 | — |
| 再認証だけを行う導線がなく、10分を過ぎると入り直しになる | クラウド上の全件削除は、直近10分以内に本人確認をしていない場合にAPIが断るようにした。画面側の受け皿がまだ無いため、クラウド保存の設定画面を作るときに、記録を保ったまま本人確認だけをやり直す導線を用意する | 未着手 | — |
| ログイン画面が、許可リストに無いメールアドレスのときも「時間をおいてもう一度」と案内する | 許可リスト拒否の応答に安定トークン `email_not_allowed` を付け、画面はこのトークンがあるときだけ入力確認の案内にする。HTTP 403 全般や通信失敗は待ち案内のまま。案内にメールアドレスと問い合わせ先は出さない。Neon Auth クライアントが未知の code を置き換えても判定できるよう、同じトークンを message にも載せる | 完了 | 2026-09-12 |
| Secret Scanの結果を秘密値に触れず定期監査で確認する経路が未確立 | 2026-09-10日次監査、YC-MON-SECRET-RESULT、重大度：未評価（結果未確認）。[Secret scanning](https://github.com/kuma-0509/yorucare/security/secret-scanning)とPush protectionは有効だが、秘密値を含む生APIは監査対象外。値を返さない件数・状態・確認日時の証跡を `pnpm security:secret-scan` で残す。取得不能と0件を区別し、警告本文は破棄することをテストで固定する。手順は `docs/security-monitoring.md` | 完了 | 2026-09-10 |
| Code scanningの解析結果を定期監査で確認できない | 2026-09-10日次監査、YC-MON-CODE-SCAN、重大度：未評価（結果未確認）。[Code scanning](https://github.com/kuma-0509/yorucare/security/code-scanning)の解析メタデータ取得は404・no analysis found。対象SHA・実行結果・日時と解析の有無を非機密の証跡として `pnpm security:code-scan` で残す。解析なしと取得不能を問題0件と区別し、警告本文は破棄することをテストで固定する。機能設定の最終確認は担当者が行う。手順は `docs/security-monitoring.md` | 完了 | 2026-09-11 |
| 公開候補の最新変更に対する必須CI合格を強制する設定を確認・整備する必要がある | 2026-09-10日次監査、YC-MON-REQUIRED-CHECKS、重大度：Medium（運用制御の不足）。GitHub取得結果ではbranchProtectionRulesと[rulesets](https://github.com/kuma-0509/yorucare/settings/rules)が空。[PR #50](https://github.com/kuma-0509/yorucare/pull/50)の先頭e91d64aにはbuild-and-test結果がなく、古いSHAの成功では代用できない。最新変更に対応するCI証跡と起動条件を確認し、必須チェック・承認・公開条件を担当者が整備して未実行・失敗時のブロックを検証する。[PR #34](https://github.com/kuma-0509/yorucare/pull/34)の開発手順統一と関連付け、実設定の証跡を残す | 未着手 | — |
| 通常利用へ移る際の入口がレビュー協力を前提とした案内のままになっている | 2026-09-13、YC-UX-NORMAL-ENTRY、UX優先度：中、セキュリティ重大度：対象外。記録開始までの負担を減らすため、運営者が検証期間の終了と通常利用への移行条件を確認したうえで、レビュー専用の同意・協力募集を通常利用の入口から分離する。匿名利用の協力を終了する場合は新規送信を止め、既存の送信停止・送信済みデータ削除の導線と必要なデータ取扱い説明を残す。受入条件：通常利用の初回・再訪でレビューへの同意を必須にせず記録へ進め、無断の送信開始や既存の削除手段の消失がないことをダミーデータで確認する | 未着手 | — |
| 保存に関する案内の量と再表示条件を、継続利用の負担に合わせて整理する必要がある | 2026-09-13、YC-UX-STORAGE-NOTICES、UX優先度：中、セキュリティ重大度：対象外。入力への集中を妨げないよう、保存方式の初回説明とバックアップを促す表示を区別し、確認済み案内の再表示条件・あとで閉じた場合の再通知間隔を整理する。現在の端末内保存に必要なデータ消失の説明、手動バックアップ・復元・削除の入口は残す。受入条件：確認済みの説明が通常の再訪で不要に割り込まず、バックアップ未実施・実施済み・延期後で意図した表示となり、保存失敗を成功に見せないことをダミーデータで確認する | 未着手 | — |
| ふりかえりタブで「面談や通院前に、伝えたいことを整理する」と予告しているが、面談準備の機能が未実装 | **記録のみ。いまは着手しない**（2026-09-15）。利用者から、これまでの記録を生成AIで整理し、それを見て自分が感じたこと、主治医や支援者へ相談したいこと、そこで得た回答を順に書き残して支援者や企業へ報告したい、という要望があった。面談1回ごとの「面談準備シート」を追加し、(1)AIの出力メモ (2)それに対して自分で思ったこと (3)相談したいこと (4)その回答 の4欄を持たせる。AIの出力はアプリから外部へ送らず、既存の共有テキストを本人がコピーして自分の生成AIへ貼り、返ってきた文章を本人が貼り戻す経路に限る（`docs/ai-consent-decision.md` の「本人が書いた本文を端末外へ出さない」方針を変えない）。保存は面談単位の独立したキーに置き、`DailyRecord` と `schemas.ts` は変えない。端末内保存と手動バックアップ・復元の対象にし、クラウド保存へは第1段階では載せない。持ち出しは本人が全文を確認したうえでのコピーと `.txt` 保存だけとし、集計情報に限っている既存の対人共有には混ぜない。空欄のまま保存できること、記録の全削除・個別削除でシートも消えること、診断・治療・評価に読める定型文をアプリ側から出さないことを表示テストで固定する。2026-09-16に画面の置き場所を決めた。体調の傾向の下にある「これから追加予定」の破線カードを面談シートのカードへ入れ替え、予告カードは廃止する（`src/components/tabs/reflection-tab.tsx`、`REFLECTION_USER_FEATURES`、`COPY.reflection.future*`）。画面案4枚（ふりかえりタブ／シートを作る／シートを書く／持ち出す）を作成して確認した。2026-09-16に画面の詳細を決めた。(1)相手の欄は置くが選択式だけとし（主治医／支援者／会社／その他）、名前や施設名を書ける自由入力は持たない。(2)1欄あたりの上限は1200字とし、コピーするまとめの先頭に「800字以内で」という依頼文を入れる。依頼文は相手のAIが守る保証がないため、上限で受け止める二段構えにする。(3)上限を超えた貼り付けは切り捨てず、残り文字数を出して減らすまで保存させない（末尾の一文が黙って消えるのを防ぐ）。(4)過去の回は一覧で並べず、「書く」タブと同じくカレンダーで1枚ずつ切り替え、前回・次回の移動も置く。(5)1日1枚に限り、日付が決まればシートが一意に決まるようにする。(6)日付の変更と、シート1枚だけの削除を用意する。削除は確認を1回はさみ、すでにシートがある日への日付変更は断る。日付を後から変えられるため、シートの識別子は日付ではなく独立したIDにする。未決は、持ち出すテキストに相手を載せるか、回答を書いたあとのシートを閉じた状態にするか、日付を選べる範囲。着手は下記「自由記述を人へ渡す扱い」の文書反映後 | 未着手 | — |
| 本人の自由記述を支援者・企業へ渡す扱いが、現行の共有方針と整合していない | **記録のみ。いまは着手しない**（2026-09-15）。`docs/sharing-decision.md` 5節は共有対象を週次まとめ由来の集計情報に限り、相談メモ・自由記述を「対象外」と定めている。面談準備シートは全欄が本人の自由記述で、主治医の回答や企業へ伝える内容を含むため、この範囲に入らない。シート専用の持ち出し経路（本人が全文を確認したうえでのコピーと `.txt` 保存）を対象外規定の例外として認めるか、経路・宛先・注意文をどう分けるかを同文書へ追記して確定する。あわせて、記録が要配慮個人情報に当たるかの確認結果（`docs/cloud-backup-consent-draft.md` 5.1節）がシートをクラウド保存へ載せられるかに効くため、判断の前提として参照する。2026-09-16に方針が決まった。シート専用の持ち出し経路は対象外規定の例外として認める。理由は、日々の記録から自動で集めた文章ではなく、本人が自分で貼り付けて入力し、本人が全文を確認したうえで渡す欄だから。残る作業は、この例外と適用範囲（シート専用経路に限り、集計情報の共有と混ぜない）を同文書5節へ書き足して確定すること | 未着手 | — |

## 実行履歴

### 2026-09-16（2）

実施タスク: 面談シートの画面詳細を決めて管理表へ反映する（開発は行わない）

詳細: 画面案を5枚へ更新し、未決だった4点を決めた。相手の欄は置くが選択式のみ（自由入力なし）。文字数は依頼文800字・欄の上限1200字の二段構えとし、超過分は切り捨てず残り文字数を出す。過去の回はカレンダーで1枚ずつ切り替え、1日1枚に限る。日付変更とシート1枚の削除を用意する。日付を変えられるため識別子は日付と別に持つ。コードは変更していない。

残課題: `docs/sharing-decision.md` 5節の改訂。持ち出すテキストに相手を載せるか、回答記入後のシートを閉じるか、日付を選べる範囲は未決。

次回候補: `docs/sharing-decision.md` 5節の改訂

### 2026-09-16

実施タスク: 面談シートの画面の置き場所を決め、自由記述の持ち出しを例外として認める方針を記録する（開発は行わない）

詳細: 画面案を4枚作って確認し、2点を決めた。(1)置き場所は体調の傾向の下で、「これから追加予定」の破線カードを面談シートのカードへ入れ替える（予告カードは廃止） (2)シート専用の持ち出し経路は、本人が貼り付けて入力し本人が全文を確認して渡す欄であることを理由に、自由記述を共有対象外とする規定の例外として認める。管理表の該当2行へ追記した。コードは変更していない。

残課題: 例外と適用範囲を `docs/sharing-decision.md` 5節へ書き足して確定すること。画面側は、相手の欄の要否、1欄あたりの文字数上限、過去シートの見せ方、シート単体の削除手段が未決。

次回候補: `docs/sharing-decision.md` 5節の改訂

### 2026-09-15

実施タスク: 面談・通院前の準備に関する利用者要望を改善タスクとして記録する（開発は行わない）

詳細: 要望は「これまでの記録を生成AIで整理し、それを見て思ったこと、相談したいこと、得た回答を順に残して支援者や企業へ報告したい」というもの。設計の分岐を4点決めた。(1)AIの出力は本人が貼り戻す方式とし、アプリから外部AIへ送らない (2)保存は日々の記録ではなく面談1回ごとのシート単位とし、`DailyRecord` と `schemas.ts` を変えない (3)持ち出しはシート専用の経路（本人が全文確認後のコピー・`.txt` 保存）とし、集計情報に限る既存の対人共有と混ぜない (4)端末内保存と手動バックアップまでを対象とし、クラウド保存へは第1段階で載せない。管理表へ2行を追加し、既存の「LLM呼び出しと報告書出力が未実装」へ経路の切り分けを1文追記した。コードは変更していない。

残課題: 自由記述を人へ渡す扱いを `docs/sharing-decision.md` 5節へ追記して確定すること。確定までシートの実装には着手しない。

次回候補: 自由記述の共有可否の判断（`docs/sharing-decision.md` 5節の改訂）

### 2026-09-14

実施タスク: 暗号化の親鍵を入れ替える手順が未文書化

選択理由: クラウド保存の第1段階が本番で動き始めたため、公開条件に残っていた親鍵入れ替え手順を先に確定する。暗号の版管理と入れ替えテストは実装済みで、文書化と全件包み直しの運用経路だけが欠けていた。YC-MON-REQUIRED-CHECKSは担当者のGitHub設定が必要。YC-UX-STORAGE-NOTICESはPR #64が未マージのため再実装しない。YC-UX-NORMAL-ENTRYは運営者の移行判断が先。再認証導線は認証フローの変更を伴うため、手順が明確な本課題を選んだ。

実装内容: 7.4節に順序と担当を書いた。包み直し計画は件数だけを出し、所有者IDと暗号文を出さない。`pnpm user-data:rewrap-keys` は確認が既定で、RLSを素通りできないロールでは止める。公開エンドポイントは増やしていない。

変更ファイル: `src/lib/server/user-data-crypto.ts` / `src/lib/server/user-data-crypto.test.ts` / `src/lib/server/user-data-key-rotation.ts` / `src/lib/server/user-data-key-rotation.test.ts` / `scripts/rewrap-user-data-keys.mjs` / `package.json` / `docs/account-cloud-storage-decision.md` / `docs/DEVELOPMENT_BOARD.md` / `docs/handoff/latest.md`

検証結果:

- lint: 成功（警告・エラーなし）
- test: 成功（73 files / 764 tests）
- build: 成功

セキュリティレビュー: 問題なし

詳細: 秘密情報の追加なし。新しい依存関係なし。公開APIなし。包み直しは記録本文を復号しない。接続文字列用の変数はVercelに置かない。アプリ用ロールでの全件0件誤認を、BYPASSRLS確認で止める。ログに所有者ID・暗号文・鍵を出さないことをテストで固定した。入口フラグは変更していない。本番データへの実行はしていない。

残課題: 再認証だけの導線は未着手。認証メールのキャリア到達確認、認証基盤のGA再確認、YC-MON-REQUIRED-CHECKS、YC-UX-NORMAL-ENTRYは運営者判断または実機が必要。YC-UX-STORAGE-NOTICESはPR #64のレビュー待ち。

次回候補: 再認証だけを行う導線（設定画面は実装済み。10分経過後の停止・退会がログイン確認画面の「ログイン済みです」で止まる）。またはYC-UX-STORAGE-NOTICES（PR #64の取り込み）。

PR: https://github.com/kuma-0509/yorucare/pull/67

### 2026-09-12

実施タスク: ログイン画面が、許可リストに無いメールアドレスのときも「時間をおいてもう一度」と案内する

選択理由: ユーザー影響のある案内の誤り。要件が明確で、転送前照合は実装済み。既存の PR #51 / #52 は 2026-09-10 時点の画面が前提で、#61 のセッション判定と衝突するため、最新 main 上で実装し直した。YC-MON-REQUIRED-CHECKS は担当者の GitHub 設定が必要なため今回は選ばない。

実装内容: 許可リスト拒否だけに `email_not_allowed` を付け、画面は待ち案内と入力確認を分ける。本文サイズ超過の 403 にはトークンを付けない。案内にメールアドレスは出さない。

変更ファイル: `src/lib/cloud-login-errors.ts` / `src/lib/cloud-login-errors.test.ts` / `src/app/api/auth/[...path]/route.ts` / `src/app/api/auth/[...path]/route.test.ts` / `src/app/cloud-login/page.tsx` / `src/app/cloud-login/page.test.tsx` / `src/lib/copy.ts` / `docs/DEVELOPMENT_BOARD.md` / `docs/handoff/latest.md`

検証結果:

- lint: 成功（警告・エラーなし）
- test: 成功（64 files / 661 tests）
- build: 成功

セキュリティレビュー: 問題なし

詳細: 秘密情報の追加なし。許可リスト照合は転送前のまま。拒否応答にメールアドレスを含めない。汎用403は許可リスト拒否にしない。入口フラグは変更していない。新しい依存関係なし。

残課題: 運営者への問い合わせ先は設定画面を作るときに決める。認証基盤側の新規登録停止は未実装のため許可リスト課題は `改善中` のまま。PR #51 / #52 は今回の実装で不要。YC-MON-REQUIRED-CHECKS と設定・復元画面は未着手。

次回候補: YC-MON-REQUIRED-CHECKS（担当者の GitHub 設定が必要。エージェントだけでは完了できない場合は保留にする）またはクラウド保存の設定・復元画面（フラグOFFのまま）

PR: https://github.com/kuma-0509/yorucare/pull/62

### 2026-09-11

実施タスク: Code scanningの解析結果を定期監査で確認できない

選択理由: P0の監視欠落。同一優先度の中で、解析なしを問題0件と書かない経路がコードで固定でき、GitHub設定の変更を要しないため。YC-MON-REQUIRED-CHECKSは担当者の設定が必要なため後回しにした。ログイン確認の許可リスト外案内は PR #51 / #52 が未マージのため再実装しない。

実装内容: `pnpm security:code-scan` で対象SHA・実行結果・日時と解析の有無だけを出す。警告本文は破棄し、404と空配列を問題0件と書かないことをテストで固定した。

変更ファイル: `src/lib/code-scan-status.ts` / `src/lib/code-scan-status.test.ts` / `scripts/code-scan-status.mjs` / `docs/security-monitoring.md` / `package.json` / `docs/DEVELOPMENT_BOARD.md` / `docs/handoff/latest.md`

検証結果:

- lint: 成功（警告・エラーなし）
- test: 成功（61 files / 630 tests）
- build: 成功
- `pnpm security:code-scan`: この環境のトークンは HTTP 403。実行結果は出さず取得不能と表示した

セキュリティレビュー: 問題なし

詳細: 警告本文・SARIF・生API応答を証跡に残さない。403と404を0件にしない。新しい依存関係なし。許可したフィールド以外が混入した場合は破棄するテストを追加した。

残課題: 権限付きトークンでの解析有無の確認と、GitHub上の機能設定の最終確認は担当者が行う。必須CI（YC-MON-REQUIRED-CHECKS）は未着手。ログイン確認の許可リスト外案内は PR #51 / #52 が未マージ。

次回候補: YC-MON-REQUIRED-CHECKS（担当者の GitHub 設定が必要。エージェントだけでは完了できない場合は保留にする）またはクラウド保存の設定・復元画面（フラグOFFのまま）

PR: https://github.com/kuma-0509/yorucare/pull/59

### 2026-09-10

実施タスク: Secret Scanの結果を秘密値に触れず定期監査で確認する経路が未確立

選択理由: P0の監視欠落。同一優先度の中で、秘密値を読まずに件数と取得不能を区別できる経路がコードで固定でき、GitHub設定の変更を要しないため。

実装内容: `pnpm security:secret-scan` で件数・状態・確認日時だけを出す。警告本文は破棄し、取得不能を0件と書かないことをテストで固定した。

変更ファイル: `src/lib/secret-scan-status.ts` / `src/lib/secret-scan-status.test.ts` / `scripts/secret-scan-status.mjs` / `docs/security-monitoring.md` / `package.json` / `docs/DEVELOPMENT_BOARD.md` / `docs/handoff/latest.md`

検証結果:

- lint: 成功（警告・エラーなし）
- test: 成功（60 files / 614 tests）
- build: 成功
- `pnpm security:secret-scan`: この環境のトークンは HTTP 403。件数は出さず取得不能と表示した

セキュリティレビュー: 問題なし

詳細: 秘密値を含む生API応答を証跡に残さない。403を0件にしない。新しい依存関係なし。警告本文が混入した場合は破棄するテストを追加した。

残課題: 権限付きトークンでの実件数確認は運営者が行う。Code scanning の解析証跡（YC-MON-CODE-SCAN）と必須CI（YC-MON-REQUIRED-CHECKS）は未着手。ログイン確認の許可リスト外案内は PR #51 / #52 が未マージ。

次回候補: YC-MON-REQUIRED-CHECKS（担当者の GitHub 設定が必要。エージェントだけでは完了できない場合は保留にする）またはクラウド保存の設定・復元画面（フラグOFFのまま）

PR: https://github.com/kuma-0509/yorucare/pull/57

