# 利用オープンデータの整理（フォーム 4-1 用）

## 現状と方針

**ヨルケアは、2026-08-17 時点でオープンデータを1件も使っていません**（リポジトリ全文検索で該当0件）。
提出要件は「最低1つ以上のオープンデータが必要」なので、ここが唯一の欠落項目です。

そこで、**アプリの弱点をオープンデータで埋める**方向で選びました。
ヨルケアには全タブ共通の相談導線がありますが、`src/lib/consultation-resources.ts` を見るとおり、
現在つながる先は **119 / 110 / #いのちSOS / 厚労省の2サイト** の5件だけで、
**利用者が住む地域の窓口へは案内できていません**。ここを都のオープンデータで置き換えます。

> **重要（この文書の限界）**
> 作業環境から `catalog.data.metro.tokyo.lg.jp` `e-stat.go.jp` `data.go.jp` `mhlw.go.jp` へは
> ネットワークが遮断されており、**データセットの実在・タイトル・中身をこちらで確認できていません**。
> 下記URLは検索結果から拾った候補です。**提出前に必ずご自身でURLを開き、タイトルと更新時点を確認**してください。
> 確認できなかったものは提出に載せず、カタログを検索して置き換えてください。

---

## 候補データセット

### ① 本命：東京都「社会福祉施設等一覧」

| | |
|---|---|
| URL（候補） | https://catalog.data.metro.tokyo.lg.jp/dataset/t000054d0000000359 |
| 別時点（候補） | https://catalog.data.metro.tokyo.lg.jp/dataset/t000054d0000000343 |
| 提供 | 東京都（福祉局） |
| 想定する中身 | 施設種別・名称・所在地・電話番号。**精神保健福祉センター**を含む |
| ヨルケアでの使い道 | 相談導線を「全国共通の5件」から「都内の実在窓口一覧」へ拡張する |
| 状況 | データ配置後に **利用中** へ変わる（`docs/hackathon2026/README.md` の Stage B） |

**確認手順（1〜2分）**
1. URLを開く。ページが存在し、タイトルが「社会福祉施設等一覧」であること
2. リソース欄にCSVがあり、その中に**精神保健福祉センター**または**保健所**の行があること
3. ライセンスが CC BY 等の再配布可能な条件であること
4. 最新時点のデータセットが別IDで出ていないか、カタログ内で「社会福祉施設等一覧」を検索して確認

### ② 併用：東京都オープンデータカタログの相談窓口・保健医療系データ

| | |
|---|---|
| 検索入口 | https://catalog.data.metro.tokyo.lg.jp/dataset |
| 探す語 | 「相談窓口」「精神保健」「こころ」「保健所」 |
| ヨルケアでの使い道 | **開所時間**の項目があれば、「平日夜間・土日に開いている窓口がどれだけ少ないか」を集計し、課題スライドの裏づけにする |
| 状況 | 利用予定 |

> 開所時間を含むデータが見つかった場合、これは**課題の裏づけとプロダクト機能の両方**に効く最も強い1件になります。優先して探してください。

### ③ 補助：総務省統計局「社会生活基本調査」

| | |
|---|---|
| 検索入口 | https://www.e-stat.go.jp/ |
| 探す語 | 「社会生活基本調査 睡眠時間 都道府県」 |
| ヨルケアでの使い道 | 東京都の睡眠時間の短さを、課題背景スライドの根拠として引用する |
| 状況 | 利用予定 |

---

## フォーム 4-1 に貼る文（確認後に調整）

登録は「データURL＋タイトル」のセットで、最大10件です。**①は必須、②③は確認できたものだけ**載せてください。

```
社会福祉施設等一覧（東京都）
https://catalog.data.metro.tokyo.lg.jp/dataset/t000054d0000000359
→ 精神保健福祉センター・保健所等の名称と連絡先を取り込み、アプリの相談導線を都内の実在窓口へ広げるために利用します。

（②で特定した相談窓口データのタイトル）
（そのURL）
→ 窓口の開所時間を集計し、平日夜間・土日に開いている窓口が少ないという課題の裏づけに利用予定です。

社会生活基本調査（総務省統計局）
（e-Statで特定したURL）
→ 東京都の睡眠時間を課題背景の根拠として利用予定です。
```

---

## 数値の扱いについて

**確認できていないデータの数値を、資料にも提出文にも書きません。**
「夜間・休日に開いている窓口は◯件だけ」といった集計値をスライドに載せるのは、
実データを取り込んで計算したあとだけにします。それまでは、
資料は「相談導線を地域の窓口へ広げる」という**設計として**書きます。

---

## Cowork への指示プロンプト

カタログのページを開いてデータセットURLが正しいことを確認したら、下のコードブロックを
Cowork にそのまま貼る。**差し替えるのは `【データセットURL】` の1行だけ。**

この作業環境から都のカタログへは届かないため、CSVの取得だけを Cowork（または手元）に頼む。
**アプリの実装・文字コード変換・Excelでの再保存はさせない。**

```
# 依頼：東京都オープンデータのCSV取得（加工しない）

## 目的
ヨルケア（働く当事者向けセルフケア記録WEBアプリ）の相談導線を、全国共通の5件から
都内の実在窓口へ広げるため、東京都オープンデータカタログのCSVを加工せずにリポジトリへ置く。

この依頼では **CSVの配置とメタデータの報告だけ** を行う。アプリ実装、集計、スライド更新はしない。

## 対象
- リポジトリ: https://github.com/kuma-0509/yorucare
- 作業ブランチ: `claude/tokyo-hackathon-2026-submission-8tsqfv`（このブランチ以外へ書かない）
- データセットURL: 【データセットURL】
- 置き場所: `data/opendata/`（無ければ作る）
- 方針の詳細: `docs/hackathon2026/open-data.md`

## やってほしいこと

1. 上のURLを開く。ページが存在し、タイトル・提供組織・ライセンス・更新時点・リソースの直リンクが取れることを確認する。
2. リソースから **CSVをダウンロード** する。複数ある場合は、施設・窓口の一覧として使える最新の1件を選ぶ。
3. **加工しない。** 文字コード変換、ExcelやNumbersでの再保存、列の削除・リネーム・フィルタ、行の間引きをしない。ダウンロードしたバイト列をそのまま保存する。
4. `data/opendata/` へ置く。ファイル名はカタログ上のリソース名を維持する（空白は `-` にしてよい。拡張子 `.csv` は変えない）。
5. 同じディレクトリに `source.md` を置き、下記「報告してほしいこと」を書く。個人情報や利用者の記録は書かない。
6. 可能ならこのブランチへコミットしてプッシュする。コミットメッセージにモデル名は書かない。例:
   `docs: 東京都オープンデータの窓口一覧CSVを配置する`
7. プッシュできない場合は、CSV と `source.md` を添付して返す。中身をチャットへ全文貼らない（大きいため）。

## ついでに（本命CSVのあと）

東京都オープンデータカタログ（CKAN API可）で、**相談窓口の開所時間** を含むデータを探す。

- 入口: https://catalog.data.metro.tokyo.lg.jp/dataset
- 検索語: 「相談窓口」「精神保健」「こころ」「保健所」「開所時間」「開館時間」
- API例: `https://catalog.data.metro.tokyo.lg.jp/api/3/action/package_search?q=相談窓口`
- 見つかったらタイトル・URL・開所時間カラムの有無だけ報告する。CSVの追加ダウンロードは、開所時間があり再配布可能なものに限る。
- 見つからなくても失敗ではない。その旨を書く。

## やってはいけないこと

- Excel / Numbers / Googleスプレッドシートで開いて保存する
- Shift_JIS などを UTF-8 へ変換して書き戻す
- 件数や「夜間・休日は◯件」などの集計値を出す（確認前の数値は提出に使わない）
- アプリ実装、`src/` の変更、スライドや提出フォーム文の更新
- 119 / 110 / `#いのちSOS` の扱いを変える提案を実装する
- 確認できていないデータセットURLを「使った」と書く
- コミットメッセージや `source.md` にモデル識別子を書く

## 報告してほしいこと

次の見出しで返す。数値の集計は不要。ヘッダーとデータは **各3行まで**。

### 本命CSV
- タイトル:
- データセットURL:
- リソース直リンク（CSVのダウンロードURL）:
- 提供組織:
- ライセンス:
- 更新時点:
- 文字コード（変換せず、BOMや `file` 等で判定した値）:
- 保存パス: `data/opendata/（ファイル名）`
- ヘッダー行（1行）:
- データ3行:

カラムの有無（○ / ×）
| 施設種別 | 名称 | 所在地 | 電話番号 | 開所時間 | 緯度経度 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

精神保健福祉センターまたは保健所の行があるか（○ / × / 未確認）:

### 開所時間データの検索
- ヒットしたデータセット（タイトルとURL。最大5件）
- 開所時間・開館時間カラムがありそうなもの
- 見つからない場合はその旨

### 作業結果
- ブランチへコミットした / 添付で返す、のどちらか
- コミットハッシュ（あれば）
```
